# Browser Configuration
browser=chrome
headless=false

# Timeout Configuration
implicit.wait=10
explicit.wait=20
page.load.timeout=30

# Base URL
base.url=https://brookfieldresidential.stg.lotvue.com

# AI Configuration
ai.api.url=https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-pro:generateContent
ai.api.key=AIzaSyC0zdFP3Aj5V4jHrxcmVbJWMyveK5N-yns
ai.model=gemini-2.0-pro
ai.temperature=0.7
ai.max.tokens=500

# Screenshot Configuration
screenshot.on.failure=true
screenshot.path=target/screenshots/

# Microsoft Teams ChatOps Configuration
# Get webhook URL from Teams channel: Channel -> Connectors -> Incoming Webhook
teams.webhook.url=your-teams-webhook-url-here
# Base URL for accessing reports (use Jenkins workspace URL or file:// for local)
report.base.url=http://localhost:8080/job/your-job-name/ws/target/cucumber-reports

# Jenkins Integration Configuration
jenkins.base.url=http://localhost:8080
jenkins.job.name=your-job-name
jenkins.username=your-username
jenkins.api.token=your-api-token

# Teams Bot Server Configuration
teams.bot.server.enabled=false
teams.bot.server.port=8080

